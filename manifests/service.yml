apiVersion: v1
kind: Service
metadata:
    name: olufemioyekanmilinetenlineten
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: olufemioyekanmilinetenlineten